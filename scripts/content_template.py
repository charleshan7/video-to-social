#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""文案源模板 —— 复制成 content.py 后填写。

这是**唯一**的文字来源：公众号和小红书的构建脚本都从这里取，
所以两个渠道的文字、图注、时间码永远一致。改文案只改这一个文件。

正文用 blocks 表达，精确控制图文顺序：
    ("p", 文本)              段落
    ("fig", (秒, 图注))       配图（秒 = 原片时间点）
    ("q",  (金句, 出处))      引语

强调分两级：
    ***文字***   品牌色加粗 —— 全文只留 5~8 处最关键的判断
    **文字**     纯黑加粗   —— 关键数字与术语首次出现，不换色
"""

# ── 基本信息 ────────────────────────────────────────────────
VIDEO = "keynote.mp4"          # 原片文件名，脚本按时间码从这里抽帧
BRAND = "#D97757"              # 品牌色。改这一行，两个渠道全篇跟着变

TITLE = "《在这里写标题》"
SOURCE_LABEL = "原视频"
SOURCE_URL = "https://..."
SOURCE_NOTE = "《原标题》· 日期 · 地点 · 时长"

# 远景幻灯片读不清时，按"秒"登记裁剪参数，抽帧时自动应用
CROPS = {
    # 287: "crop=in_w*0.368:in_h*0.345:in_w*0.010:in_h*0.238",
}

# ── 导语：每条一段，控制在 84 字内 ───────────────────────────
LEDE = [
    "第一句交代时间地点和基本事实。",
    "第二句给出这篇为什么值得读。",
    "第三句抛出全文主线。",
]

# ── 静态目录：(编号, 标题, 起始时间码) ────────────────────────
TOC = [
    ("01", "第一节标题", "02:54"),
    ("02", "第二节标题", "10:20"),
]
TOC_TAIL = "一句话概括全文主线"

# ── 正文：按讲者或议题分 5~8 节 ───────────────────────────────
SECTIONS = [
    dict(no="01", tc=174, title="第一节标题", blocks=[
        ("fig", (140, "人物图｜姓名、职务、一句背景介绍")),
        ("p", "段落文字，每段控制在 84 字内（15px 下约 4 行）。"),
        ("p", "关键数字用 **12 万** 这样标；全场最关键的判断用 ***三星号*** 标。"),
        ("q", ("一句值得单独拎出来的原话。", "讲者姓名")),
        ("fig", (258, "信息图｜图注要说清这张图在讲什么")),
    ]),
]

# ── 收尾 ────────────────────────────────────────────────────
CODA = [
    "回头看，全文主线其实是……",
    "**如果只能带走一件事……**",
]
ENDING = "适合做收尾的那句金句。"
ENDING_BY = "出处"

FOOTER = ("本文为解读，非逐字实录：带引号处为讲者原话中译，其余为整理者归纳。"
          "配图取自原片截帧，版权归原作者所有，此处作评述引用；图注标注原片时间码，可回原片核对。")

# ── 小红书正文（≤950 字，标签也算） ──────────────────────────
XHS_COPY = """第一句要抓人，信息流里只显示前两行。

第二段给核心论点。

几个印象最深的：

1｜……
2｜……
3｜……

一句收束。

你怎么看？评论区聊聊 👇

#标签1 #标签2 #标签3"""
